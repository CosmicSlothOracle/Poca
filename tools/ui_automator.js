/* Minimal placeholder to document the UI automation entrypoint.
 * A full Playwright harness isn't bundled here to avoid adding deps.
 * Run the app with `npm run dev` and attach your own Playwright runner.
 */
console.log('UI automator placeholder. Start the dev server with `npm run dev` and run your Playwright tests separately.');


